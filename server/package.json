{
  "name": "server",
  "version": "1.0.0",
  "license": "ISC",
  "main": "index.js",
 "scripts": {
  "generate": "prisma generate",
  "build": "npm i && npm run generate && tsc --build",
  "start": "node ./dist/index.js",
  "build:dev": "tsc --build --watch",
  "start:dev": "node --watch ./dist/index.js",
  "dev": "npm run generate && nodemon ./index.ts"
},
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
